"""
OpenMemory MCP Server

This is a simple MCP server with tools to interact with OpenMemory.

It servers as the runner and bridge to long term memory and user context.
"""

import asyncio

class OpenMemoryMCPServer:
    """
    OpenMemory MCP Server
    """

    def __init__(self):
        """
        Initialize the OpenMemory MCP Server
        """
        pass
    
    async def run(self) -> None:
        """
        Run the OpenMemory MCP Server
        """
        pass

if __name__ == "__main__":
    asyncio.run(OpenMemoryMCPServer().run())